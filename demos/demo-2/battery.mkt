// Energy storage system node representing home battery storage
//
// Tracks battery charge percentage and current operating mode

service battery {
  // Current battery charge level as a percentage from 0 to 100
  var charge_level: int = 85;

  // Operational mode: "idle", "charging", or "discharging"
  var status: string = "charging";
}
